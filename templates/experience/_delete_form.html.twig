<form method="post" action="{{ path('experience_delete', {'id': experience.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette Expérience');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experience.id) }}">
    <button class="btn ui red button">Supprimer</button>
</form>
