{% extends 'base.html.twig' %}

{% block title %}Edit Experience{% endblock %}

{% block body %}
    <div class="ui container">
        <h3>Editer {{ experience.title }}</h3>
        {{ include('experience/_form.html.twig', {'button_label': 'Editer'}) }}
    </div>

{% endblock %}
